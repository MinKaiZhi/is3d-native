batch_size: 2
steps: 200
lr: 0.0001
grad_accum_steps: 4
use_bf16_amp: true
checkpoint_segments: true
checkpoint_every_steps: 0
checkpoint_keep_last: 0
log_every: 20
snapshot_dir: artifacts/nan_snapshots

# Real-data training config (CO3D WebDataset shards)
train_shards: D:/datasets/co3dv2_wds/shard-*.tar
train_key_include: __images__
val_shards: null
val_key_include: __images__
dataloader_workers: 4
wds_shuffle_buffer: 1024
val_wds_shuffle_buffer: 0

# Validation controls (set val_shards + eval_every_steps to enable periodic validation)
eval_every_steps: 0
val_batches: 16

image_size: 128
in_channels: 3

fastvit_embed_dim: 256
fastvit_depth: 6
fastvit_num_heads: 8
fastvit_patch_size: 8

triplane_depth: 4
triplane_num_heads: 8
triplane_channels: 24
triplane_resolution: 32
triplane_token_mask_ratio: 0.15
